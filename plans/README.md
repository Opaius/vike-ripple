# Implementation Plans

Generated by the improve skill on 2026-06-26 at commit `0dd9f8a`. Execute in
the order below unless dependencies say otherwise. Each executor: read the
plan fully before starting, honor its STOP conditions, and update your row
when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Add characterization tests for @cioky/ripple-query-remult | P1 | L | — | DONE |
| 002  | Replace deterministic BETTER_AUTH_SECRET with per-scaffold random secret | P1 | S | — | DONE |
| 003  | Serialize query cache in streaming SSR path | P1 | M | 004 (flushPending fix) | DONE |
| 004  | Fix core query() invalidation + implement GC + scope pending per-request | P1 | M | 001 | DONE |
| 005  | Fix useLiveQuery — cleanup SSE sub, handle errors, set error signal | P2 | S-M | — | TODO |
| 006  | Add framework-agnostic infinite query to ripple-query core | P2 | M | 001 | TODO |

## Dependency notes

- **001 before 004 and 006**: characterization tests for query-remult give
  the executor a verification baseline before changing core query behavior
  (004 changes invalidate/GC; 006 adds a new export). Run 001 first.
- **004 before 003**: plan 003 (streaming cache) calls `flushPending()`;
  plan 004 fixes `flushPending()` to be per-request (the module-level
  `pending` array is a cross-request race). If 003 lands first, the
  streaming path's `flushPending()` call is still racy. Land 004's
  `pending` fix first, then 003.
- **002 and 005 are independent**: the secret fix and the live-query fix
  touch unrelated files. Run them in parallel with anything.
- **006 depends on 001**: the infinite query tests in 001 characterize the
  existing Remult version; 006 adds the agnostic version. Having the
  characterization tests first means the executor can confirm the Remult
  version still works after the core package gets a new export.

## Findings considered and rejected

- *serializeKey memoization / invalidateKeys O(n) / onRenderHtml per-render
  allocations*: micro-optimizations with no evidence of a hot-path
  bottleneck at realistic cache sizes. Not worth doing.
- *knip `ignoreDependencies` masking bugs*: `knip` runs clean (exit 0) —
  the ignore entries are cosmetic, not masking real unused-dep errors.
- *Dir1 (plugin auto-patch `import 'ripple-query-remult/plugin'`)*: old
  design idea, scrapped by the maintainer. Not planning.
- *Missing child AGENTS.md for 3 packages (finding #12)*: real DOX gap but
  not in the selected plan set — the maintainer chose correctness/security
  first. Can be done independently as housekeeping.
- *Lint failing (finding #13)*: pre-existing diagnostics (organizeImports +
  remult-partykit formatting). Not bundled into any plan — each plan's lint
  gate checks for NEW diagnostics only. Fix separately as a one-off
  `bun run lint:fix` if desired.
- *Redundant `@tsrx/ripple` root devDep (finding #14)*: confirmed unused
  (zero source imports it). Can be removed in a one-line edit outside these
  plans. Not worth a plan file.
- *`useQuery`/`useInfiniteQuery` never clean up `registerInvalidator`
  (finding #7)*: real bug, same class as plan 005. Not in the selected set
  — can be folded into a follow-up with 005 if the maintainer wants it.
- *Dead `.tsrx` hook twins (finding #10)*: real tech-debt. Not in the
  selected set — can be done as a quick deletion pass once the `.ts` files
  are confirmed canonical.
- *Create CLI `name` arg unsanitized / path traversal (finding #16)*: low
  risk (local CLI, `npm install` at cwd). Can be a quick guard addition
  outside these plans.
- *No automated SSR/routing integration test (finding #18)*: real gap, L
  effort. Not in the selected set — would be a good standalone plan if the
  maintainer wants CI regression protection.
